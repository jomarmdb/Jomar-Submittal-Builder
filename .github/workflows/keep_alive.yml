name: Keep Jomar App Awake

on:
  schedule:
    # Runs every 4 hours
    - cron: '15 */4 * * *'
  workflow_dispatch: 

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send Keep-Alive Ping
        run: curl -ILs https://jomar-submittal-builder.streamlit.app/
