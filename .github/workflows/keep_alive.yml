name: Keep Jomar App Awake

on:
  schedule:
    - cron: '0 */3 * * *'
  workflow_dispatch: 

jobs:
  wake-up:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4 # Latest version as of 2026

      - name: Set up Python
        uses: actions/setup-python@v5 # Latest version as of 2026
        with:
          python-version: '3.11'

      - name: Install Selenium
        run: pip install selenium webdriver-manager

      - name: Run Human-Simulated Browser Visit
        run: |
          python - <<EOF
          from selenium import webdriver
          from selenium.webdriver.chrome.options import Options
          from selenium.webdriver.chrome.service import Service
          from webdriver_manager.chrome import ChromeDriverManager
          import time

          options = Options()
          options.add_argument("--headless=new")
          options.add_argument("--no-sandbox")
          options.add_argument("--disable-dev-shm-usage")
          options.add_argument("user-agent=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Safari/537.36")

          service = Service(ChromeDriverManager().install())
          driver = webdriver.Chrome(service=service, options=options)

          try:
              print("Waking up the Jomar Submittal Builder...")
              driver.get("https://jomar-submittal-builder.streamlit.app/")
              
              # Wait for the page to load
              time.sleep(12)
              
              # Scroll to simulate human activity
              driver.execute_script("window.scrollTo(0, 500);")
              time.sleep(3)
              
              print(f"Success! App is awake. Title: {driver.title}")
          except Exception as e:
              print(f"Encountered an issue: {e}")
          finally:
              driver.quit()
          EOF
